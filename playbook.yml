---
- hosts: all
  vars:
  roles:
  tasks:
    - name: Update Apt-Cache
      apt:
        update_cache: true
        cache_valid_time: 3600
      become: true
      when: ansible_os_family == "Debian"
